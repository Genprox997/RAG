# RAG Project — Hybrid Retrieval + Agentic RAG (cloud LLM)
# Core
openai>=1.40.0
python-dotenv>=1.0.0
numpy>=1.26.0
tiktoken>=0.7.0
# Vector store
faiss-cpu>=1.8.0
# Local embeddings
fastembed>=0.3.0
# Keyword retrieval
rank_bm25>=0.2.2
# Optional PDF ingestion (priority: PyMuPDF > pdfplumber > pypdf; OCR needs paddleocr)
PyMuPDF>=1.24.0
pdfplumber>=0.11.0
pypdf>=4.2.0
# paddleocr>=2.7.0  # optional, only for scanned-PDF OCR fallback
# UI
streamlit>=1.36.0
# Evaluation
# (uses the same openai client; no extra deps)
